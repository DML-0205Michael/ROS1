local_costmap:
  global_frame: odom
  robot_base_frame: base_link
  static_map: false # use temp map scanned by lidar
  rolling_window: true # local costmap follow robot?
  width: 3
  height: 3
  update_frequency: 10.0 # wsa 5 when using amcl
  publish_frequency: 10 # wsa 5 when using amcl
  transform_tolerance: 2.0